export const metadata = {
  title: "MedCircle",
  description: "Your Acute Medicine Assistant",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
